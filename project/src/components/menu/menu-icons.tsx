


//Diccionario : nombre -> componente

import { LayoutDashboard, Settings, User } from "lucide-react";

export const IconMap: Record<string, React.ElementType> = {
    home: LayoutDashboard,
    users: User,
    settings: Settings
}